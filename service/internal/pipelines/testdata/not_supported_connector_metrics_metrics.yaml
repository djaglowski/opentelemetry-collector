receivers:
  nop:
exporters:
  nop:
connectors:
  bf:

service:
  pipelines:
    metrics/in:
      receivers: [nop]
      exporters: [bf]
    metrics/out:
      receivers: [bf]
      exporters: [nop]

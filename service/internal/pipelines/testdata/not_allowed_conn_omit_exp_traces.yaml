receivers:
  nop:
exporters:
  nop:
connectors:
  nop/conn:

service:
  pipelines:
    traces/in:
      receivers: [nop]
      exporters: [nop]
    traces/out:
      receivers: [nop, nop/conn]
      exporters: [nop]

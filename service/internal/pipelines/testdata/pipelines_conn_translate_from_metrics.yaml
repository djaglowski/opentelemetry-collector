receivers:
  examplereceiver:

processors:
  exampleprocessor:

exporters:
  exampleexporter:

connectors:
  exampleconnector:

service:
  pipelines:
    metrics:
      receivers: [examplereceiver]
      processors: [exampleprocessor]
      exporters: [exampleconnector]
    traces:
      receivers: [exampleconnector]
      processors: [exampleprocessor]
      exporters: [exampleexporter]
    logs:
      receivers: [exampleconnector]
      processors: [exampleprocessor]
      exporters: [exampleexporter]

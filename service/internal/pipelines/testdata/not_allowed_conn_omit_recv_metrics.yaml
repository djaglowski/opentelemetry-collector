receivers:
  nop:
exporters:
  nop:
connectors:
  nop/conn:

service:
  pipelines:
    metrics/in:
      receivers: [nop]
      exporters: [nop, nop/conn]
    metrics/out:
      receivers: [nop]
      exporters: [nop]
